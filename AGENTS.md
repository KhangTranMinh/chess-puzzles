# Repository instructions

## GitHub authentication

- For authenticated GitHub operations, load `GITHUB_TOKEN` from the local `.env` file.
- Never print, commit, or persist the token in Git configuration, remotes, logs, or source files.
- Keep `.env` untracked.
