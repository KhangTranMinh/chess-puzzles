# Backend package for Chess Puzzle Viewer.
#
# This file makes the package explicit.  Without it, Python 3.3+ uses implicit
# namespace packages, which works but can confuse tooling and older environments.
